---
title: Vue Project
description: 'Complete guide for installing and using Bitrix24 icons in Vue applications.'
---

## Installation

### Adding to Vue Project

Install the Bitrix24 Icons package using your preferred package manager:

::code-group{sync="pm"}

```bash [pnpm]
pnpm add @bitrix24/b24icons-vue
```

```bash [yarn]
yarn add @bitrix24/b24icons-vue
```

```bash [npm]
npm install @bitrix24/b24icons-vue
```

```bash [bun]
bun add @bitrix24/b24icons-vue
```

::

## Usage Examples

### Basic Usage
Import icons as Vue-components.

::component-example
---
name: 'icon-import-example'
collapse: true
---
::

### Generic Icon Component

Use the [B24Icon](https://github.com/bitrix24/b24icons/blob/main/packages/@bitrix24-icons-vue/src/components/B24Icon.ts) component for easy access to icons.

The component uses dynamic imports via `import()` to load icons as needed.

This does not break the `tree-shaking` principle because `tree-shaking` works with static imports, while dynamic imports
are used for load optimization, loading only the necessary parts of the code when they are needed.

> Specify the full icon name with the `name` parameter, e.g., `Main::CopilotAiIcon`.

::component-example
---
name: 'icon-component-example'
collapse: true
---
::
